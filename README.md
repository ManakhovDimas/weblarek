# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

##### Данные

В приложении используются две основные сущности данных, которые описываются следующими интерфейсами:

 Интерфейсы данных

 Товар (Product)

interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

Назначение: Описывает структуру данных товара в приложении. Содержит все необходимые поля для отображения и работы с товаром в каталоге и корзине.

• id: уникальный идентификатор товара (строка).
• description: описание товара (строка).
• image: URL-адрес изображения товара (строка).
• title: название товара (строка).
• category: категория, к которой относится товар (строка).
• price: цена товара (число или null, если товар без цены).

 Покупатель (Buyer)

type TPayment = 'online' | 'offline' | '' ;

interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
}

Назначение: Описывает структуру данных покупателя, которые необходимы для оформления заказа. Содержит контактную информацию и предпочтения по оплате.

• payment: тип оплаты, выбранный покупателем (тип TPayment).
• email: адрес электронной почты покупателя (строка).
• phone: номер телефона покупателя (строка).
• address: адрес доставки (строка).

Интерфейс запроса на создание заказа

interface IOrderRequest {
  payment: string;   
  email: string;     
  phone: string;     
  address: string;   
  total: number;     
  items: string[];   
}

Назначение: Представляет структуру данных, которую клиент отправляет на сервер для создания нового заказа. Это основной DTO (Data Transfer Object) для оформления покупки.

• payment: Определяет выбранный пользователем способ оплаты (строка).
• email: Электронная почта для связи с покупателем (строка).
• phone: Телефон для связи с покупателем (строка).
• address: Адрес доставки товаров(строка).
• total: Общая стоимость заказа (число).
• items: Список идентификаторов товаров в заказе (строка).

Интерфейс ответа сервера

interface IOrderResponse {
  id: string;     // ID созданного заказа
  total: number;  // Подтвержденная сумма заказа
}

Назначение: Представляет структуру данных, которую сервер возвращает клиенту после успешного создания заказа.

• id : Уникальный идентификатор созданного заказа (строка).
• total : Подтвержденная сервером сумма заказа (число).



Модели данных

###### Класс Catalog (Каталог товаров)

Назначение: Отвечает за хранение и управление списком всех доступных товаров в приложении, а также за хранение товара, выбранного для подробного просмотра.

Поля класса:

1 products: IProduct[] - массив товаров
   – Тип: IProduct[] (массив объектов типа IProduct)
   – Инициализация: пустой массив []
   – Назначение: хранит все товары каталога

2 selectedProduct: IProduct | null - выбранный товар
   – Тип: IProduct | null (объект типа IProduct или null)
   – Инициализация: null
   – Назначение: хранит текущий выбранный товар (например, при просмотре деталей товара)


Методы класса:
• saveProducts(products: IProduct[]): void - сохраняет массив товаров, полученный в параметрах метода
• getProducts(): IProduct[] - возвращает массив всех товаров из модели
• getProductById(id: string): IProduct | undefined - возвращает товар по его id
• setSelectedProduct(product: IProduct): void - сохраняет товар для подробного отображения
• getSelectedProduct(): IProduct | null - возвращает товар для подробного отображения

###### Класс Cart (Корзина)

Назначение: Отвечает за хранение и управление товарами, которые пользователь выбрал для покупки. Реализует логику работы с корзиной.

Поля класса:
items: IProduct[] = []
-items (тип: IProduct[]) - массив товаров для инициализации корзины (по умолчанию пустой массив)

Методы класса:
• getItems(): IProduct[] - возвращает массив товаров, которые находятся в корзине
• addItem(item: IProduct): void - добавляет товар, полученный в параметре, в массив корзины
• removeItem(productId: string): void - удаляет товар, полученный в параметре, из массива корзины
• clear(): void - очищает корзину (удаляет все товары)
• getTotalPrice(): number - возвращает общую стоимость всех товаров в корзине
• getItemCount: number - возвращает количество товаров в корзине
• hasItem(productId: string): boolean - проверяет наличие товара в корзине по его id, полученному в параметре метода

###### Класс Buyer (Покупатель)

Назначение: Отвечает за хранение и управление данными покупателя, которые необходимы для оформления заказа.

Конструктор:
constructor(data: Partial<IBuyer> = {})

• data (тип: Partial<IBuyer>) - частичные данные покупателя для инициализации (по умолчанию пустой объект)

Поля класса:
• payment (тип: TPayment) - хранит вид оплаты, выбранный покупателем
• email (тип: string) - хранит email покупателя
• phone (тип: string) - хранит телефон покупателя
• address (тип: string) - хранит адрес доставки покупателя

Методы класса:
• setData(data: Partial<IBuyer>): void - сохраняет данные покупателя. Метод позволяет сохранить как все данные сразу, так и отдельные поля, не удаляя при этом значения других полей
• getData(): IBuyer - возвращает все данные покупателя в виде объекта
• clearData(): void - очищает все данные покупателя (сбрасывает все поля к значениям по умолчанию)
• validateData(): Partial<Record<keyof IBuyer, string>> - Метод для валидации данных покупателя.Возвращает объект с ошибками, где ключ - поле, значение - сообщение об ошибке

###### Слой коммуникации

▎Описание класса DataApiClient

Назначение: Будет служить слоем коммуникации между приложением и сервером «Веб-ларёк». Он будет использовать методы get и post из интерфейса IApi для получения и отправки данных.

Конструктор клиента API

constructor(api: IApi) - Реализация интерфейса IApi для выполнения HTTP-запросов

▎Методы класса:
- getCatalog(): Promise<IProduct[]> - получает массив товаров с эндпоинта /product/ и возвращает 
объект с полями total:number и items: IProduct[].
- createOrder(orderData: IOrderRequest): Promise<IOrderResponse> отправляет заказ на эндпоинт /order/.Сервер создает заказ и возвращает его с полями Id:string и подтвержденную сумму total: number



###### Слой представления (View)

###### Класс Modal
Назначение: Управление модальными окнами приложения. Обеспечивает открытие, закрытие и управление содержимым модальных окон.

Конструктор:

constructor(container: HTMLElement)

- `container` (тип: `HTMLElement`) - DOM-элемент, который будет использоваться как контейнер модального окна

Поля класса:
- `_container: HTMLElement` - корневой DOM-элемент модального окна
- `_content: HTMLElement | null` - текущее содержимое модального окна
- `_closeButton: HTMLButtonElement | null` - кнопка закрытия модального окна

Методы класса:
- `open(): void` - открывает модальное окно
- `close(): void` - закрывает модальное окно
- `setContent(content: HTMLElement): void` - устанавливает содержимое модального окна
- `on(event: string, callback: Function): void` - подписывается на события модального окна
- `emit(event: string, ...args: any[]): void` - генерирует события модального окна

События:
- `modal:open` - генерируется при открытии модального окна
- `modal:close` - генерируется при закрытии модального окна

---

###### Класс Card (базовый класс для карточек)
Назначение: Абстрактный базовый класс для всех карточек товаров. Определяет общую структуру и методы для работы с карточками.

Конструктор:

constructor(container: HTMLElement)

- `container` (тип: `HTMLElement`) - DOM-элемент-контейнер карточки

Поля класса:
- `_image?: HTMLImageElement` - элемент изображения товара
- `_title: HTMLElement` - элемент заголовка товара
- `_category?: HTMLElement` - элемент категории товара
- `_price: HTMLElement` - элемент цены товара
- `_button?: HTMLButtonElement` - кнопка действия на карточке

Методы класса:
- `setCardImage(src: string, alt?: string): void` - устанавливает изображение товара
- `setTitle(title: string): void` - устанавливает заголовок товара
- `setCategory(category: string): void` - устанавливает категорию товара с соответствующим CSS-классом
- `setPrice(price: number | null): void` - устанавливает цену товара (отображает "Бесценно" для `null`)
- `setButtonText(text: string): void` - устанавливает текст на кнопке
- `setButtonDisabled(disabled: boolean): void` - включает/выключает кнопку
- `setButtonHandler(handler: () => void): void` - устанавливает обработчик клика на кнопку



###### Класс CatalogItem
Назначение: Отображение товара в каталоге. Наследуется от `Card<IProduct>`.

Интерфейс действий:

interface ICatalogItemActions {
    onClick: (event: MouseEvent) => void;
}


Фабричная функция:

function createCatalogItem(data: IProduct, actions?: ICatalogItemActions): CatalogItem


Особенности реализации:
- Использует шаблон `#card-catalog` из HTML
- Обрабатывает клики по всей карточке товара
- Отображает базовую информацию о товаре: изображение, название, категорию, цену

---

###### Класс ProductPreview
Назначение: Детальное отображение товара в модальном окне. Наследуется от `Card<IProduct>`.

Интерфейс действий:

interface IProductPreviewActions {
    onAddToCart: () => void;
    onRemoveFromCart: () => void;
}


Дополнительные поля:
- `_description: HTMLElement` - элемент описания товара
- `_isInCart: boolean` - флаг, указывающий находится ли товар в корзине

Методы класса:
- `setInCart(value: boolean): void` - обновляет состояние "в корзине"
- `setDescription(description: string): void` - устанавливает описание товара
- `updateButton(): void` - обновляет текст кнопки в зависимости от состояния товара в корзине

Фабричная функция:

function createProductPreview(data: IProduct, isInCart: boolean, actions?: IProductPreviewActions): ProductPreview



###### Класс BasketItem
Назначение: Отображение отдельного товара в корзине.

Интерфейс действий:

interface IBasketItemActions {
    onDelete: () => void;
}


Поля класса:
- `_index: HTMLElement` - элемент порядкового номера товара
- `_title: HTMLElement` - элемент названия товара
- `_price: HTMLElement` - элемент цены товара
- `_deleteButton: HTMLButtonElement` - кнопка удаления товара из корзины

Методы класса:
- `setIndex(index: number): void` - устанавливает порядковый номер товара
- `setTitle(title: string): void` - устанавливает название товара
- `setPrice(price: number): void` - устанавливает цену товара

Фабричная функция:

function createBasketItem(item: IProduct, index: number, actions?: IBasketItemActions): BasketItem


###### Класс BasketView
Назначение: Отображение всей корзины покупок с товарами и общей стоимостью.

Интерфейс действий:

interface IBasketViewActions {
    onOrder: () => void;
    onDeleteItem: (item: IProduct) => void;
}


Поля класса:
- `_list: HTMLElement` - элемент списка товаров
- `_totalPrice: HTMLElement` - элемент общей стоимости
- `_orderButton: HTMLButtonElement` - кнопка оформления заказа
- `_emptyMessage: HTMLElement` - сообщение о пустой корзине
- `_items: BasketItem[]` - массив элементов корзины

Методы класса:
- `setItems(items: IProduct[]): void` - устанавливает список товаров в корзине
- `setTotalPrice(price: number): void` - устанавливает общую стоимость товаров
- `setOrderButtonEnabled(enabled: boolean): void` - включает/выключает кнопку оформления заказа
- `renderEmpty(): void` - отображает состояние пустой корзины

Фабричная функция:

function createBasketView(actions?: IBasketViewActions): BasketView


###### Класс OrderForm
Назначение: Форма первого шага оформления заказа (адрес доставки и способ оплаты).

Интерфейс действий:

interface IOrderFormActions {
    onPaymentChange: (payment: TPayment) => void;
    onAddressChange: (address: string) => void;
    onSubmit: () => void;
}


Поля класса:
- `_paymentButtons: NodeListOf<HTMLButtonElement>` - кнопки выбора способа оплаты
- `_addressInput: HTMLInputElement` - поле ввода адреса доставки
- `_nextButton: HTMLButtonElement` - кнопка перехода к следующему шагу
- `_errorsElement: HTMLElement` - элемент для отображения ошибок валидации
- `_selectedPayment: TPayment` - выбранный способ оплаты

Методы класса:
- `setPaymentMethod(method: TPayment): void` - устанавливает способ оплаты
- `setAddress(address: string): void` - устанавливает адрес доставки
- `setErrors(errors: ValidationErrors<IBuyer>): void` - отображает ошибки валидации
- `getData(): Partial<IBuyer>` - возвращает данные, введенные в форму
- `validateForm(): void` - выполняет валидацию данных формы

Фабричная функция:

function createOrderForm(actions?: IOrderFormActions): OrderForm



###### Класс ContactsForm
Назначение: Форма второго шага оформления заказа (контактные данные покупателя).

Интерфейс действий:

interface IContactsFormActions {
    onEmailChange: (email: string) => void;
    onPhoneChange: (phone: string) => void;
    onSubmit: () => void;
}


Поля класса:
- `_emailInput: HTMLInputElement` - поле ввода email
- `_phoneInput: HTMLInputElement` - поле ввода телефона
- `_submitButton: HTMLButtonElement` - кнопка отправки формы
- `_errorsElement: HTMLElement` - элемент для отображения ошибок валидации

Методы класса:
- `setEmail(email: string): void` - устанавливает email
- `setPhone(phone: string): void` - устанавливает телефон
- `setErrors(errors: ValidationErrors<IBuyer>): void` - отображает ошибки валидации
- `getData(): Partial<IBuyer>` - возвращает данные, введенные в форму
- `validateForm(): void` - выполняет валидацию данных формы

Фабричная функция:

function createContactsForm(actions?: IContactsFormActions): ContactsForm



###### Класс SuccessView
Назначение: Отображение успешного оформления заказа с информацией о сумме заказа.

Интерфейс действий:

interface ISuccessViewActions {
    onClose: () => void;
}


Поля класса:
- `_title: HTMLElement` - элемент заголовка успешного оформления
- `_description: HTMLElement` - элемент описания с суммой заказа
- `_closeButton: HTMLButtonElement` - кнопка закрытия окна успеха

Методы класса:
- `setTotal(total: number): void` - устанавливает сумму успешно оформленного заказа

Фабричная функция:

function createSuccessView(actions?: ISuccessViewActions): SuccessView




###### Слой презентера (Presenter)

###### Класс App
Назначение: Главный координатор приложения, реализующий логику Presenter. Управляет взаимодействием между View и Model слоями.

Перечисление событий приложения:

export enum AppEvents {
    CATALOG_LOADED = 'catalog:loaded',
    PRODUCT_SELECTED = 'product:selected',
    PRODUCT_ADDED_TO_CART = 'product:added',
    PRODUCT_REMOVED_FROM_CART = 'product:removed',
    CART_UPDATED = 'cart:updated',
    ORDER_STARTED = 'order:started',
    ORDER_SUBMITTED = 'order:submitted',
    ORDER_COMPLETED = 'order:completed',
    MODAL_OPEN = 'modal:open',
    MODAL_CLOSE = 'modal:close'
}


Поля класса:
- `_modal: Modal` - экземпляр модального окна
- `_catalog: Catalog` - модель каталога товаров
- `_cart: Cart` - модель корзины покупок
- `_buyer: Buyer` - модель данных покупателя
- `_apiClient: DataApiClient` - клиент для работы с API
- `_gallery: HTMLElement` - DOM-элемент контейнера каталога
- `_basketButton: HTMLButtonElement` - кнопка открытия корзины
- `_basketCounter: HTMLElement` - счетчик товаров в корзине

Основные методы:

###### Инициализация приложения:
- `constructor()` - инициализирует все компоненты приложения
- `setupEventListeners(): void` - настраивает обработчики событий для всех компонентов
- `loadCatalog(): Promise<void>` - загружает каталог товаров с сервера

###### Работа с каталогом товаров:
- `renderCatalog(): void` - отображает каталог товаров на странице
- `openProductPreview(product: IProduct): void` - открывает детальный просмотр товара в модальном окне

###### Работа с корзиной покупок:
- `addToCart(product: IProduct): void` - добавляет товар в корзину
- `removeFromCart(product: IProduct): void` - удаляет товар из корзины
- `updateBasketCounter(): void` - обновляет счетчик товаров в корзине
- `openBasket(): void` - открывает корзину покупок в модальном окне
- `updateBasketDisplay(basketView: BasketView): void` - обновляет отображение корзины

###### Оформление заказа:
- `openOrderForm(): void` - открывает форму первого шага оформления заказа
- `validateOrderStep1(): boolean` - выполняет валидацию данных первого шага заказа
- `openContactsForm(): void` - открывает форму второго шага оформления заказа
- `validateOrderStep2(): boolean` - выполняет валидацию данных второго шага заказа
- `submitOrder(): Promise<void>` - отправляет заказ на сервер
- `showSuccess(total: number): void` - отображает окно успешного оформления заказа


###### Архитектурные паттерны

###### 1. Фабричный метод (Factory Method)
Каждый компонент представления имеет соответствующую фабричную функцию:
- `createCatalogItem()` - создает элемент каталога
- `createProductPreview()` - создает детальный просмотр товара
- `createBasketItem()` - создает элемент корзины
- `createBasketView()` - создает представление корзины
- `createOrderForm()` - создает форму заказа
- `createContactsForm()` - создает форму контактов
- `createSuccessView()` - создает представление успеха

###### 2. Наблюдатель (Observer)
Реализован через систему событий `EventEmitter`:
- Компоненты подписываются на события через метод `on()`
- Генерируют события через метод `emit()`
- Класс `App` координирует взаимодействие через обработку событий

###### 3. Шаблонный метод (Template Method)
- Класс `Card` является абстрактным базовым классом с общими методами
- Конкретные классы (`CatalogItem`, `ProductPreview`) наследуются от `Card` и переопределяют специфичное поведение

###### 4. Компоновщик (Composite)
- Класс `BasketView` содержит массив элементов `BasketItem`
- Управляет отображением и взаимодействием всех элементов корзины как единого целого

---

###### Потоки данных в приложении

###### Поток добавления товара в корзину:
1. Пользователь кликает на товар в каталоге
2. `CatalogItem` генерирует событие `PRODUCT_SELECTED`
3. `App` обрабатывает событие и вызывает метод `openProductPreview()`
4. Создается экземпляр `ProductPreview` с кнопкой "В корзину"
5. Пользователь кликает на кнопку "В корзину"
6. `ProductPreview` вызывает обработчик `onAddToCart`
7. `App` обрабатывает событие `PRODUCT_ADDED_TO_CART`
8. `App` добавляет товар в модель `Cart`
9. `App` генерирует событие `CART_UPDATED`
10. Обновляется счетчик товаров в корзине в пользовательском интерфейсе

###### Поток оформления заказа:
1. Пользователь открывает корзину и нажимает кнопку "Оформить заказ"
2. `BasketView` вызывает обработчик `onOrder`
3. `App` обрабатывает событие `ORDER_STARTED`
4. `App` открывает `OrderForm` (первый шаг оформления)
5. Пользователь заполняет адрес доставки и выбирает способ оплаты
6. При каждом изменении данные сохраняются в модель `Buyer`
7. Пользователь нажимает кнопку "Далее"
8. `App` выполняет валидацию данных и открывает `ContactsForm` (второй шаг)
9. Пользователь заполняет контактные данные (email и телефон)
10. Пользователь нажимает кнопку "Оформить заказ"
11. `App` отправляет данные заказа на сервер через `apiClient`
12. При успешной отправке очищаются корзина и данные покупателя
13. Отображается `SuccessView` с информацией о сумме заказа



###### Особенности реализации

###### Управление состоянием
- Состояние корзины покупок хранится в модели `Cart`
- Данные покупателя хранятся в модели `Buyer`
- Класс `App` синхронизирует состояние между представлением и моделями

###### Валидация данных
- Двухэтапная валидация при оформлении заказа
- `validateOrderStep1()` - проверяет корректность адреса и выбора способа оплаты
- `validateOrderStep2()` - проверяет корректность email и телефона
- Ошибки валидации отображаются в соответствующих формах

###### Обработка ошибок
- Использование try-catch блоков при работе с API
- Консольное логирование ошибок для отладки
- Graceful degradation при ошибках загрузки данных

###### Производительность
- Ленивая загрузка каталога товаров
- Локальное кэширование данных для уменьшения запросов к серверу
- Минимальные перерисовки DOM для оптимизации производительности

